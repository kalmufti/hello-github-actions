name: A workflow for my Hello World file
on: push
jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest    
    steps:
    - name: Github repo
      run: echo "${{ github.repository }}"
    - name: Github workkspace
      run: echo "{{ GITHUB_WORKSPACE }}"
    - uses: actions/checkout@v1
    - uses: ./action-a
      env:
        MY_NAME: "Khalid"
