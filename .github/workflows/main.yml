name: A workflow for my Hello World file
on: push
jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest    
    steps:
    - name: Testing
      env:
        EVENT_CONTEXT: ${{ github.event }}
    - uses: actions/checkout@v1
    - uses: ./action-a
      env:
        MY_NAME: "Khalid"
